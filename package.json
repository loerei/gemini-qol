{
  "name": "gemini-qol",
  "version": "1.0.0",
  "type": "module",
  "description": "Chrome extension providing QoL improvements for Google Gemini",
  "main": "content.js",
  "scripts": {
    "test": "node test/markdown.test.js",
    "build": "esbuild src/index.js --bundle --outfile=content.js",
    "watch": "esbuild src/index.js --bundle --outfile=content.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
